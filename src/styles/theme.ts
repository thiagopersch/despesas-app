import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0393BE",
    },
    secondary: {
      main: "#0DBF87",
    },
  },
});

export default theme;
